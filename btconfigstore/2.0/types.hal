package vendor.qti.hardware.btconfigstore@2.0;

enum Result : int32_t {
    SUCCESS,
    UNKNOWN_ERROR,
};

struct ControllerAddOnFeaturesList {
    uint16_t product_id;
    uint16_t rsp_version;
    uint8_t feat_mask_len;
    uint8_t[8] features;
};

struct HostAddOnFeaturesList {
    uint8_t feat_mask_len;
    uint8_t[8] features;
};
